apiVersion: v1
kind: Service
metadata:
  name: my-devops-app-service
spec:
  type: NodePort
  selector:
    app: my-devops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080